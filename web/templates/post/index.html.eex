<%= for post <- @posts do %>

  <h2>
    <%= link post.title, to: post_path(@conn, :show, post) %>
  </h2>
  <p>
    <%= post.content %>
  </p>

<% end %>

<%= pagination_links @conn, @posts, path: &post_path/3 %>
